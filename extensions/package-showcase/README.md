# Package Showcase

This extension demonstrates the V2 package APIs:

- `app.manifest.get()`
- `app.extension.package()`
- `app.assets.text(path)`
- `app.assets.json(path)`
- `app.assets.url(path)`

It is intentionally small and self-contained so extension authors can copy it
as a starting point.
