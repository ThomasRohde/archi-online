# Selection Workbench

Tracks recent selection changes and exposes selection-aware context-menu
commands. It demonstrates event handlers, context menu contributions,
extension-private storage, commands, and panels.
