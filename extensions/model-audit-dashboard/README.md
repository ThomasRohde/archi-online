# Model Audit Dashboard

Runs lightweight checks against the current model and shows the result in a
dockable panel. It demonstrates commands, toolbar buttons, menu items, panels,
packaged JSON data, packaged SVG assets, and extension-private storage.
