# Event Log Console

Shows the app event bridge from an extension author point of view. It listens
to lifecycle, model, view, tree, and selection events, then renders the recent
event stream in a dockable panel.
